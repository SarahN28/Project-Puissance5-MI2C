#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int pivot(){
  int rp;                         //rp : nom du diamètre du pivot
  rp = rand()%2;
  if(rp==0){                      //diamètre du pivot = 3 cases
    rp = 3;                       
  }
  else{                          //diamètre du pivot = 5 cases
  rp = 5;
  }
printf("Le pivot a un diamètre de %d cases\n", rp);
return rp;
}
int main(){
  int rp = 88;
  int tab[6][8]={0};
  srand(time(NULL));
  rp = pivot();
  return 0;
 }
